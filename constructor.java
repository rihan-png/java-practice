//Constructor is a special method that automatically runs when an object is created.
class student
 // This is a default (no-argument) constructor. 
    // It is automatically called when an object is instantiated (e.g., Student s = new Student();)
  {
    student()
    {
      system.out.println("created");
    }
  
